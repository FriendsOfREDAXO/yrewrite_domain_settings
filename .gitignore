# General
.DS_Store
.idea

# Composer pulls dev tooling only - the addon ships without either, and
# installer_ignore keeps them out of the package as well.
/vendor/
/composer.lock

# Local tool output
/.php-cs-fixer.cache
/.playwright-mcp/

# Generated per installation by domain-settings:ide-helper - it lists the field
# names of whatever instance last cleared its cache, so it does not belong here.
/.phpstorm.meta.php
